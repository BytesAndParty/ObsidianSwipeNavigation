{
	"name": "obsidian-swipe-navigation",
	"version": "1.0.0",
	"description": "Desktop trackpad swipe gestures for navigating back and forward in Obsidian",
	"main": "main.js",
	"scripts": {
		"dev": "bun run esbuild.config.mjs",
		"build": "bun tsc -noEmit -skipLibCheck && bun run esbuild.config.mjs production"
	},
	"keywords": [
		"obsidian",
		"navigation",
		"trackpad",
		"gestures",
		"swipe",
		"desktop"
	],
	"author": "Robert Stickler",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.10.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.19.0",
		"obsidian": "latest",
		"typescript": "^5.3.0"
	}
}
